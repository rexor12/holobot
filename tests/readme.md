These are some temporary low effort tests to ensure that at least parts of the various features work as expected.

Running:
1. Update the `__main__.py` file in the `tests` to load the module you want to run.
2. `py -m tests`
